#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""Раскладка собранной семантики по темам статей.

Вордстат отдаёт один материал десятком формулировок: «как избавиться от
тревоги», «как справиться с тревогой», «как справиться с тревожностью».
Отдать это писателям как есть — получить десять статей, которые отбирают
трафик друг у друга. Здесь такие формулировки сходятся в одну тему: самая
частотная становится заголовком, остальные — ключевыми словами, которые
должны быть употреблены в тексте.

Тема определяется тремя уровнями:

1. **Объект** — о чём запрос. Берётся из сида выгрузки и нормализуется:
   тревога = тревожность = беспокойство, гнев = злость = ярость.
2. **Тип вопроса** — признаки, причины, что делать, тест, у кого. Меняет
   материал, поэтому «причины тревоги» и «как справиться с тревогой»
   остаются разными статьями.
3. **Уточнение** — слово, ради которого стоит отдельная статья: у женщин,
   на работе, после родов, в школе.

Уровень уточнения существует потому, что двух уровней мало: группа
«психолог + обзор» без него собрала 2185 запросов — от «психолог в школе»
до «психолог-нарколог». Крупные группы дробятся, пока тема не станет
размером со статью.

Запросы без явного типа вопроса не выбрасываются: они идут обзорной темой
об объекте. Прежний отсев по шаблону «информационный запрос» уносил вместе
с мусором рабочие темы вроде «психолог в школе» и «закрыть гештальт».

Запуск:
    python3 scripts/group_topics.py [порог частоты]     # по умолчанию 300
"""
from __future__ import annotations

import csv
import glob
import re
import sys
from collections import defaultdict

SRC = "docs/seo/raw/wordstat-2026-08-31/СВОДКА-целевая.csv"   # --src переопределяет
ARTICLES = "docs/seo/articles"
OUT = "docs/seo/temy-sgruppirovannye.md"

MAX_KW = 40      # больше ключей в одну статью не уложить — группа дробится
MIN_SUB = 3      # уточнение заводит подтему, если держит столько запросов
MIN_N = 1000     # ниже этой частоты тема оправдана только объёмом ключей
DEPTH = 5        # глубже дробить нечего: уточнения кончаются

# Блоки лендинга и продукт, на который блок ведёт (доли — из pravila-otbora-tem.md)
BLOCKS = {
    "01": ("Отношения и близость", "/artterapy"),
    "02": ("Словарь и термины", "/artterapy"),
    "03": ("Тревога и депрессия", "/artterapy"),
    "04": ("Эмоции и состояния", "/artterapy"),
    "05": ("Дети и подростки", "/artterapy"),
    "06": ("Самооценка и личность", "/artterapy"),
    "07": ("Выгорание и работа", "/coach"),
    "08": ("Для психологов", "/praktik"),
    "09": ("Практики и тесты", "/online"),
    "10": ("Медитации и практики", "/online"),
}

SEED_BLOCK = {
    "01": """абьюз газлайтинг виктимблейминг буллинг моббинг созависимость контрзависимость
        кризис-в-отношениях отношения отношения-с-мужчиной психология-отношений привязанность
        базовое-доверие ревность развод как-пережить-развод расставание как-пережить-расставание
        сепарация сепарация-от-родителей кризис-сепарации симбиоз слияние соперничество обида
        одиночество чувство-одиночества отверженность ненужность эмпатия зависть""",
    "02": """абулия алекситимия амбивалентность ангедония бессознательное вытеснение гештальт
        незавершённый-гештальт фигура-и-фон деперсонализация дереализация диссоциация депривация
        защитные-механизмы индивидуация инсайт интроекция интроспекция катарсис катастрофизация
        когнитивные-искажения компенсация копинг контрперенос перенос локус-контроля лабильность
        отрицание ошибка-выжившего проекция рационализация регрессия резильентность рефлексия
        ригидность руминация самоактуализация сублимация суперэго эго расщепление соматизация
        триггер флешбэк фрустрация эффект-даннинга-крюгера выученная-беспомощность психосоматика
        невроз ипохондрия""",
    "03": """тревога тревожность повышенная-тревожность паническая-тревожность паника беспокойство
        страх финансовая-тревожность депрессия признаки-депрессии послеродовая-депрессия апатия
        отчаяние""",
    "04": """гнев злость ярость раздражение грусть печаль тоска горе вина стыд стыд-за-себя
        неловкость смущение скука разочарование опустошённость онемение-чувств эмоциональные-качели
        перепады-настроения импульсивность подавленные-эмоции как-прожить-эмоцию
        как-справиться-с-эмоция почему-я-чувствую-эмоция эмоция-причины бессилие беспомощность
        обесценивание""",
    "05": """ребенок воспитание-ребенка детская-психология детский-психолог истерика-у-ребенка
        подросток подростковый-возраст переходный-возраст самооценка-ребенка""",
    "06": """самооценка низкая-самооценка самооценка-женщины как-повысить-самооженку самокритика
        внутренний-критик перфекционизм как-повысить-самооценку""",
    "07": """выгорание эмоциональное-выгорание профессиональное-выгорание синдром-выгорания
        профилактика-выгорания прокрастинация-чувств синдром-самозванца деньги
        отношение-к-деньгам""",
    "08": """психолог психолог-онлайн клинический-психолог семейный-психолог консультация-психолога
        образование-психолога работа-психологом психотерапия семейная-психотерапия
        когнитивная-психотерапия методы-психотерапии""",
    "09": """тест-на-депрессию тест-на-тревожность саморегуляция""",
    "10": """медитация медитировать осознанность релаксация аффирмация
        дыхательная дыхательные-практики""",
}
BLOCK_OF = {s.replace("-", " "): nn
            for nn, ss in SEED_BLOCK.items() for s in ss.split()}

# Сиды-синонимы сводим к одному объекту, иначе «тревога» и «тревожность»
# дадут две статьи под один запрос.
SEED_NORM = {
    "тревожность": "тревога", "повышенная тревожность": "тревога",
    "паническая тревожность": "тревога", "беспокойство": "тревога",
    "финансовая тревожность": "тревога о деньгах",
    "признаки депрессии": "депрессия", "тест на депрессию": "депрессия",
    "тест на тревожность": "тревога",
    "низкая самооценка": "самооценка", "самооценка женщины": "самооценка",
    "как повысить самооценку": "самооценка", "самооценка ребенка": "самооценка",
    "эмоциональное выгорание": "выгорание", "профессиональное выгорание": "выгорание",
    "синдром выгорания": "выгорание", "профилактика выгорания": "выгорание",
    "как пережить расставание": "расставание", "как пережить развод": "развод",
    "гнев": "злость", "ярость": "злость",
    "печаль": "грусть", "тоска": "грусть",
    "чувство одиночества": "одиночество",
    "стыд за себя": "стыд", "самокритика": "внутренний критик",
    "подростковый возраст": "подросток", "переходный возраст": "подросток",
    "детская психология": "детский психолог",
    "психолог онлайн": "психолог", "клинический психолог": "психолог",
    "семейный психолог": "психолог", "консультация психолога": "психолог",
    "образование психолога": "психолог", "работа психологом": "психолог",
    "семейная психотерапия": "психотерапия", "когнитивная психотерапия": "психотерапия",
    "методы психотерапии": "психотерапия",
    "как прожить эмоцию": "эмоции", "как справиться с эмоция": "эмоции",
    "почему я чувствую эмоция": "эмоции", "эмоция причины": "эмоции",
    "подавленные эмоции": "эмоции", "онемение чувств": "эмоции",
    "перепады настроения": "эмоциональные качели",
    "отношения с мужчиной": "отношения", "психология отношений": "отношения",
    "кризис в отношениях": "отношения", "воспитание ребенка": "ребенок",
    "сепарация от родителей": "сепарация", "кризис сепарации": "сепарация",
    "незавершённый гештальт": "гештальт", "отношение к деньгам": "деньги",
    "выученная беспомощность": "беспомощность", "бессилие": "беспомощность",
    "прокрастинация чувств": "прокрастинация",
}

# Тип вопроса. Порядок важен: срабатывает первый совпавший.
ANGLES = [
    ("тест",         r"\bтест\w*|опросник|шкал[аыуе]\b|пройти"),
    ("упражнения",   r"упражнен|техник|практик|методик|способ|как научиться|инструмент|"
                     r"что помогает"),
    ("признаки",     r"признак|симптом|проявл|как выглядит|как проявляется"),
    ("как понять",   r"как понять|как определить|как узнать|как распознать|как выявить|"
                     r"как отследить|как проверить"),
    ("что делать",   r"что делать|как справ|как избав|как побор|как перестат|как пережит|"
                     r"как помочь|как борот|как выйти|как убрат|как снят|как снизит|"
                     r"как отпустит|как поднят|как повысит|как полюбит|как принят|как прожит|"
                     r"как вернут|как останов|как восстанов|как закрыт|как выраз|как контролир|"
                     r"как наладит|как выразить|что делать если"),
    ("отличия",      r"отлич|разниц|чем не\b"),
    ("для близких",  r"как поддержат|что говорит|как общат|как жить с|если муж|если жена|"
                     r"если партн|как вести себя с|как помочь близк"),
    ("причины",      r"причин|почему|отчего|откуда|из-за чего|зачем"),
    ("сколько",      r"сколько|как долго|стади|этап|фаз[аыу]\b|степен|виды|типы|уровн"),
    ("можно ли",     r"^можно ли|^нужно ли|^стоит ли|^надо ли|^бывает ли|^лечится ли"),
    ("определение",  r"что такое|простыми словами|это что|значение|означает|определение|кто так"),
]

# Слова типа вопроса и служебные — на объект запроса не влияют.
ANGLE_WORDS = re.compile(
    r"\b(тест\w*|опросник|шкал\w*|пройти|упражнен\w*|техник\w*|практик\w*|методик\w*|способ\w*|"
    r"признак\w*|симптом\w*|проявл\w*|выглядит|как|что|такое|делать|справ\w*|избав\w*|побор\w*|"
    r"перестат\w*|пережит\w*|помочь|борот\w*|выйти|убрат\w*|снят\w*|снизит\w*|отпустит\w*|"
    r"поднят\w*|повысит\w*|полюбит\w*|принят\w*|прожит\w*|вернут\w*|останов\w*|восстанов\w*|"
    r"закрыт\w*|выраз\w*|контролир\w*|наладит\w*|понять|определить|узнать|распознать|выявить|"
    r"отследить|проверить|отлич\w*|разниц\w*|или|причин\w*|почему|отчего|откуда|зачем|сколько|"
    r"долго|стади\w*|этап\w*|фаз\w*|степен\w*|уровн\w*|виды|вид|типы|тип|поддержат\w*|говорит\w*|"
    r"общат\w*|жить|вести|можно|нужно|стоит|надо|бывает|лечится|ли|это|простыми|словами|значение|"
    r"означает|определение|кто|какой|какая|какие|список|значит|сделать|самостоятельно|"
    r"у|в|во|на|с|со|по|для|и|а|о|об|от|до|при|же|за|к|не|ни|но|мне|себя|свой|своих|его|её|ее)\b"
)

# Словоформы лемматизатора и заведомо чужое. В список тем такое пускать нельзя.
DROP = re.compile(
    r"^(признаки|причины|симптомы|виды|этапы|стадии) (психолог|самооценка|паника|депрессия|"
    r"тревожность|эмоция|ребенок|отношения)\b|"
    r"^\w+ (какое|какой|каком|какая|ответы|вопрос|тесте|теста)\b|"
    r"\b(тесте|теста|тестов|эмоций|эмоция)$|"
    r"вакансии|зарплат|работа психолог|работу психолог|устроит|резюме|подработ|"
    r"купить|цена|стоимость|скидк|заказать|доставка|интернет.?магазин|"
    r"знакомств|анкет[аыу]|"
    r"поступить|вуз|колледж|диплом|переподготовк|бакалавр|магистрат|егэ|"
    r"скачать|торрент|бесплатно смотреть|онлайн смотреть|"
    r"прыщ|дермат|похуден|диет[аыу]|алимент|госпошлин|квартир|имуществ|"
    r"фильм|сериал|дорам|аниме|манга|песн|клип|мем|актер|актрис|цитат|"
    r"смотреть|серия|сезон|дом 2|бренд|авито|недорого|рейтинг|телефон|адрес|"
    r"круглосуточно|отзыв|шоу|игра|музык|конкурс|олимпиад|реферат|курсов[ао]й|"
    r"луиза хей|синельников|idrlabs|idrla|тик ?ток|"
    r"великая депрессия|диаграмм|круговая|"
    r"мкб|инвалидност|больничн|справк|таблетк|препарат|лекарств|"
    r"^\d|^[a-z]+$|"
    r"как впасть|как начать депресс|как заработать депресс|как подавлять|как заглушить чувства|"
    # соматика: «горечь во рту» и «психосоматика цистита» — не наша тема
    r"во рту|цистит|геморро|кож[аиу]|горл|поясниц|спин[аыу]|ше[яи]|желуд|кишечн|"
    r"давлени|температур|сопл|насморк|нерв|печен|почк|сустав|позвоночн|зуб|глаз|"
    r"ух[оа]|уши|аллерг|рак\b|опухол|гастрит|язв|мигрен|тошнот|изжог|запор|понос|"
    r"грыж|защемл|пятк|ноги|рук[аи]|живот|матк|яичник|простат|"
    # поиск услуги по городу и цене, а не чтение
    r"москв|спб|санкт|екатеринбург|новосибирск|казан|краснодар|нижн|рядом|"
    r"запис[ья]|прием|приём|сколько стоит|стоит психолог|где найти|клиника|"
    # профориентация и обучение на психолога
    r"после 9|после 11|класса|университет|институт|профессия|кем работать|"
    r"сколько зарабат|учиться на|программа педагог|аттестац|категори|"
    # именные клинические шкалы
    r"\bбек[аи]?\b|hads|госпитальн|маслач|спилберг|цунга|гамильтон|эдинбург|"
    r"рубинштейн|дембо|филипс|люшер|орлов|"
    # ищут конкретного человека, а не тему
    r"психолог (елена|ирина|анна|ольга|натал|мари|светлан|татьян|екатерин|юли|"
    r"алекс|дмитр|сергей|андрей|игор|ник|викт|окса|вера|дарь|полин|аню|лариса)|"
    # смежные профессии и медицина у детей — не наш блог
    r"нарколог|психиатр|невролог|логопед|дефектолог|брюшин|"
    r"(симптом|лечени|болезн|температур)\w*\s+\w*\s*(детей|ребен|ребён|подростк)|"
    r"(детей|ребен|ребён|подростк)\w*\s+(симптом|лечени|болезн)|"
    r"обесценивание (денег|рубл|валют)|инфляц|"
    # запросы методистов и педагогов: нормативка, а не родительская боль
    r"требовани|организаци|образовательн|фгос|методическ|патриотическ|нравственн|"
    r"духовн|конституц|законодательн|нормативн|пособи|родительское собрание|"
    r"федеральн|государственн|муниципальн|дошкольн|инклюзивн|воспитательн|"
    r"социализац|компетенц|учреждени|садик|детском саду|"
    # эстрада и медиаперсоны
    r"валери|пьеха|киркоров|дорофеев|шоу.?бизнес|"
    # вторая волна: лечебная гимнастика, гуру и файлы вместо тем
    r"стрельников|щетинин|бутейко|цигун|йог[аиу]|лфк|логопед|урологи|"
    r"диспенз|вальяк|синельник|хей|правдина|блаво|"
    r"слушать|скачать|музык|звуки|минусовк|аудио|плейлист|торрент|"
    r"свеч[аиу]|благовони|поющие чаши|"
    r"(депресси|невроз|тревожн|психоз|шизофрен)\w*\s+у\s+(подростк|детей|ребен|ребён)",
    re.I,
)

# Эзо-фильтр: на этих словах падает сборка блога, тема бессмысленна.
EZO = re.compile(
    r"магия|магическ|волшебн|гадани|гадат|таро|рун[аы]\b|гороскоп|астролог|эзотерик|"
    r"предсказани|предсказат|карма|чакр|биополе|привлечение денег|привлечение любви|"
    r"исполнение желани|вселенн|порч|сглаз|заговор|приворот|расклад|снится|сонник|"
    r"скорпион|козерог|близнец|телец|овен|овна|водоле|стрелец|стрельц|"
    r"\bдев[аы]\b|\bвесы\b|\bрак[оу]?м?\b|\bльв?[оа]?м?\b|\bрыб[ыа]\b|знак зодиак",
    re.I,
)

# Заголовком темы такое ставить нельзя, а ключом — можно: это либо склейка
# лемматизатора («причина ребенок»), либо сервисный хвост запроса.
NOT_HEAD = re.compile(
    r"^(причина|причины|симптом|симптомы|признак|признаки|тест|теста|тесте|вид|виды|"
    r"этап|этапы|стади[яи]|шкала|уровень) \w+$|"
    r"^\w+ (это|после|сколько|года|лет|онлайн|бесплатно|отзывы|форум|видео|фото)$|"
    r"\b(бесплатн\w*|платн\w*|онлайн|отзывы|форум|видео|фото|сайт|скачать)\b|"
    r"^\w+ \w+ (это|онлайн|бесплатно)$",
    re.I,
)

# Служебные хвосты: их наличие не заводит отдельную статью.
WEAK = {
    "такое", "значи", "прост", "слова", "может", "чего", "этого", "своих",
    "года", "лет", "время", "сейча", "нужно", "можно", "делат", "стать",
    "онлай", "беспл", "быстр", "дома", "самос", "списо", "каким", "котор",
    "синдр", "чувст", "состо", "ощуще", "пробл", "вообщ", "форум", "сайт",
    "перио", "момен", "случа", "сторо", "смысл", "слово", "терми", "понят",
    "прове", "делаю", "делае", "лучши", "перву", "прием", "видео", "книга",
    "смотр", "фото", "карти", "текст", "пример", "новый", "перес", "разбо",
    # организационно-канцелярские хвосты: своей статьи не заводят
    "проце", "услов", "основ", "являе", "участ", "план", "вопро", "цель",
    "задач", "форма", "роль", "значе", "особе", "харак", "аспек", "факто",
    "элеме", "компо", "содер", "средс", "принц", "тема", "работ", "деяте",
    "систе", "разви", "уровн", "качес", "резул", "напра", "подхо", "этапы",
}


# Под этими пятибуквенными корнями прячутся разные слова: «психо» — это и
# психолог, и психосоматика, и психотерапия; «расст» — и расставание, и
# расстройство; «после» — и предлог, и «послеродовая». Их режем длиннее.
AMBIG = {"психо", "расст", "после"}


# Уточнения, ради которых стоит отдельная статья: та же тема, но другой
# читатель или другой контекст.
STRONG = {
    "женщи", "мужчи", "девуш", "детей", "ребен", "ребён", "подро", "взрос",
    "мамы", "матер", "родит", "мужа", "жены", "партн", "близк", "школе",
    "семье", "браке", "доу", "саду", "работ", "утром", "ночью", "вечер",
    "берем", "родов", "первы", "хрони", "остры", "зимой", "весно", "разво",
}


def roots(text: str) -> set:
    """Корни значимых слов, обрезанные до пяти букв.

    Ровно до пяти, а не «длина минус два»: при плавающей длине «тревоги»
    давало «трево», «тревожностью» — «тревож», и один материал попадал
    в список дважды.
    """
    s = ANGLE_WORDS.sub(" ", text.lower())
    return {w[:8] if w[:5] in AMBIG else w[:5]
            for w in re.findall(r"[а-яёa-z0-9]+", s) if len(w) > 2}


def angle_of(q: str) -> str:
    """Тип вопроса. Запрос без явного типа — не мусор, а обзорная тема."""
    for name, pat in ANGLES:
        if re.search(pat, q):
            return name
    return "обзор"


def covered_intents() -> list:
    """Пары «объект + тип вопроса», уже занятые опубликованными статьями.

    Берём заголовок, главный ключ и LSI-ключи: статья закрывает тему и тогда,
    когда её запрос стоит не в заголовке, а в списке ключей.
    """
    out = set()
    for path in glob.glob(f"{ARTICLES}/*.md"):
        head = open(path, encoding="utf-8").read(3000)
        qs = re.findall(r"^(?:title|primaryKeyword):\s*\"?([^\"\n]+)", head, re.M)
        lsi = re.search(r"^lsiKeywords:\n((?:\s+-\s.+\n)+)", head, re.M)
        if lsi:
            qs += re.findall(r"-\s*\"?([^\"\n]+)", lsi.group(1))
        strong = len(re.findall(r"^(?:title|primaryKeyword):", head, re.M))
        for i, q in enumerate(qs):
            q = q.strip().lower().split(":")[0]
            out.add((angle_of(q), frozenset(roots(q) - WEAK), i < strong))
    return list(out)


def split(items: list, banned: set, depth: int = 0) -> list:
    """Дробит группу на темы размером со статью.

    Ядро — запросы без выраженного уточнения; каждое уточнение, за которое
    держатся хотя бы MIN_SUB запросов, отходит в свою тему.
    """
    if len(items) <= MAX_KW or depth >= DEPTH:
        return [items]

    holders = defaultdict(list)
    for it in items:
        for r in it["mods"] - banned:
            holders[r].append(it)
    subs = {r: v for r, v in holders.items() if len(v) >= MIN_SUB}
    if not subs:
        return [items]

    # Сильнейшее уточнение забирает свои запросы первым, дальше — по остатку.
    core, taken, out = list(items), set(), []
    for r in sorted(subs, key=lambda r: -sum(i["n"] for i in subs[r])):
        part = [i for i in subs[r] if id(i) not in taken]
        if len(part) < MIN_SUB:
            continue
        taken.update(id(i) for i in part)
        out += split(part, banned | {r}, depth + 1)
    core = [i for i in core if id(i) not in taken]
    if core:
        out.append(core)
    return out


def main() -> None:
    args = [a for a in sys.argv[1:] if not a.startswith("--")]
    floor = int(args[0]) if args else 300
    csv_out = next((a.split("=", 1)[1] for a in sys.argv[1:]
                    if a.startswith("--csv=")), None)
    src = next((a.split("=", 1)[1] for a in sys.argv[1:]
                if a.startswith("--src=")), SRC)

    groups = defaultdict(list)
    seen_q, kept, skipped = set(), 0, defaultdict(int)
    for r in csv.DictReader(open(src, encoding="utf-8")):
        q, n = r["phrase"].strip().lower(), int(r["frequency"])
        seed = r["seed"].strip().replace("-", " ")
        if n < floor or q in seen_q:
            continue
        seen_q.add(q)
        nn = BLOCK_OF.get(seed)
        if not nn:
            skipped["сид вне блоков"] += 1
            continue
        if len(q.split()) < 2:
            skipped["одно слово"] += 1
            continue
        if DROP.search(q):
            skipped["мусор и чужие темы"] += 1
            continue
        if EZO.search(q):
            skipped["эзотерика"] += 1
            continue
        obj = SEED_NORM.get(seed, seed)
        mods = roots(q) - roots(obj) - WEAK
        groups[(nn, obj, angle_of(q))].append({"q": q, "n": n, "mods": mods})
        kept += 1

    topics, tail = [], defaultdict(list)
    for (nn, obj, a), items in groups.items():
        items.sort(key=lambda i: -i["n"])
        for part in split(items, set()):
            part.sort(key=lambda i: -i["n"])
            # Что не уложилось в статью — хвост: разнородные низкочастотные
            # запросы, которые ни во что не группируются. В темы их не пишем,
            # но и не теряем: они лежат отдельным разделом файла.
            if len(part) > MAX_KW:
                tail[obj] += part[MAX_KW:]
                part = part[:MAX_KW]
            # Тема на паре низкочастотных формулировок статьи не стоит.
            if part[0]["n"] < MIN_N and len(part) < 3:
                tail[obj] += part
                continue
            head = next((i for i in part if not NOT_HEAD.search(i["q"])), None)
            if head is None:
                tail[obj] += part
                continue
            topics.append({"nn": nn, "obj": obj, "a": a, "items": part,
                           "head": head["q"], "n": head["n"]})
    topics.sort(key=lambda t: -t["n"])

    # Одна пара «объект + тип вопроса» — одна статья. Темы, собранные из разных
    # сидов, могут сойтись на одном материале: «шкала тревоги и депрессии» из
    # блока про депрессию и «тест на тревожность и депрессию» из блока тестов.
    # Такие сливаем в самую частотную, иначе две статьи поделят один запрос.
    WEAK5 = {w[:5] for w in WEAK}
    STRONG5 = {w[:5] for w in STRONG}
    by_sig, merged_dup = {}, 0
    for t in topics:
        sig = (t["a"], frozenset(roots(t["head"]) - WEAK5))
        host = by_sig.get(sig)
        if host is None:
            by_sig[sig] = t
        else:
            host["items"] += t["items"]
            t["dropped"] = True
            merged_dup += 1
    # Второй проход: «воспитание ребёнка» и «процесс воспитания ребёнка» —
    # это одна статья, разошлись они на служебном слове.
    topics = [t for t in topics if not t.get("dropped")]
    order = sorted(topics, key=lambda t: -t["n"])
    for i, x in enumerate(order):
        if x.get("dropped"):
            continue
        xr = roots(x["head"])
        for y in order[i + 1:]:
            if y.get("dropped") or y["a"] != x["a"] or y["nn"] != x["nn"]:
                continue
            yr = roots(y["head"])
            # Общий предмет есть, а расходятся только служебными словами:
            # «воспитание ребёнка» и «процесс воспитания ребёнка».
            if (xr & yr) - WEAK5 and (xr ^ yr) <= WEAK5:
                x["items"] += y["items"]
                y["dropped"] = True
                merged_dup += 1
    topics = [t for t in topics if not t.get("dropped")]
    for t in topics:
        if len(t["items"]) > MAX_KW:
            t["items"].sort(key=lambda i: -i["n"])
            tail[t["obj"]] += t["items"][MAX_KW:]
            t["items"] = t["items"][:MAX_KW]

    # Тема считается закрытой, если объект и тип вопроса сошлись с заголовком,
    # главным или LSI-ключом опубликованной статьи.
    done = covered_intents()
    for t in topics:
        mine = frozenset(roots(t["head"]) - WEAK)
        t["covered"] = any(a == t["a"] and mine == ks
                           and (strong or t["a"] != "обзор")
                           for a, ks, strong in done)

    by_block = defaultdict(list)
    for t in topics:
        by_block[t["nn"]].append(t)

    free = [t for t in topics if not t["covered"]]
    with open(OUT, "w", encoding="utf-8") as f:
        f.write(f"# {len(topics)} тем под статьи\n\n")
        f.write(
            f"Выгрузка Вордстата от 31.08.2026: {kept} запросов от {floor} показов "
            f"в месяц разложены по {len(topics)} темам.\n"
            f"Из них {len(free)} тем ещё не закрыты статьями блога.\n\n"
            "**Связанные запросы объединены в одну тему.** «как избавиться от тревоги»,\n"
            "«как справиться с тревогой» и «как справиться с тревожностью» — это один\n"
            "материал; все формулировки идут в колонку «ключевые слова» и должны быть\n"
            "употреблены в тексте статьи.\n\n"
            "Колонка «есть» — тема уже закрыта опубликованной статьёй.\n\n")
        i = 0
        for nn in sorted(by_block):
            name, cta = BLOCKS[nn]
            n_free = sum(1 for t in by_block[nn] if not t["covered"])
            f.write(f"\n## {nn}. {name} — {len(by_block[nn])} тем "
                    f"({n_free} свободны) → {cta}\n\n")
            f.write("| # | Тема | Частота | Есть | Ключевые слова в статью |\n"
                    "|---|---|---|---|---|\n")
            for t in by_block[nn]:
                i += 1
                kws = " · ".join(it["q"] for it in t["items"])
                mark = "да" if t["covered"] else ""
                f.write(f"| {i} | {t['head']} | {t['n']} | {mark} | {kws} |\n")

        pairs = []
        order2 = sorted(topics, key=lambda t: -t["n"])
        for i, x in enumerate(order2):
            xr = frozenset(roots(x["head"]) - WEAK5)
            for y in order2[i + 1:]:
                yr = frozenset(roots(y["head"]) - WEAK5)
                if x["a"] == y["a"] and x["nn"] == y["nn"] \
                        and xr & yr and len(xr ^ yr) == 1:
                    pairs.append((x, y, "".join(xr ^ yr)))
        if pairs:
            f.write(f"\n\n## Близкие темы — {len(pairs)} пар на проверку\n\n")
            f.write("Один предмет и один тип вопроса, расходятся одним словом.\n"
                    "Где это аудитория или контекст («в школе», «у женщин») — темы\n"
                    "разные. Где оттенок формулировки — свести в одну статью.\n\n")
            f.write("| Тема | Частота | Близкая тема | Частота | Расходятся |\n"
                    "|---|---|---|---|---|\n")
            for x, y, d in sorted(pairs, key=lambda p: -p[0]["n"]):
                f.write(f"| {x['head']} | {x['n']} | {y['head']} | {y['n']} | {d} |\n")

        if tail:
            n_tail = sum(len(v) for v in tail.values())
            f.write(f"\n\n## Хвост — {n_tail} запросов без своей темы\n\n")
            f.write("Разнородные низкочастотные формулировки: в статью их не\n"
                    "уложить, но и терять не нужно — пойдут ключами в тексты по\n"
                    "своему объекту, когда те будут писаться.\n\n")
            f.write("| Объект | Запросов | Формулировки |\n|---|---|---|\n")
            for obj in sorted(tail, key=lambda o: -len(tail[o])):
                qs = sorted(tail[obj], key=lambda i: -i["n"])
                f.write(f"| {obj} | {len(qs)} | "
                        f"{' · '.join(i['q'] for i in qs)} |\n")

    n_tail = sum(len(v) for v in tail.values())
    if csv_out:
        with open(csv_out, "w", encoding="utf-8", newline="") as fh:
            w = csv.writer(fh)
            w.writerow(["блок", "название_блока", "cta", "тема", "частота",
                        "тип_вопроса", "объект", "покрыто", "ключи"])
            for t in topics:
                w.writerow([t["nn"], BLOCKS[t["nn"]][0], BLOCKS[t["nn"]][1],
                            t["head"], t["n"], t["a"], t["obj"],
                            "да" if t["covered"] else "",
                            " · ".join(i["q"] for i in t["items"])])
        print(f"→ {csv_out}")

    sizes = sorted(len(t["items"]) for t in topics)
    print(f"запросов разложено {kept - n_tail} → тем {len(topics)}, "
          f"из них свободных {len(free)}; в хвосте {n_tail}")
    print(f"слито повторов на финальной сверке: {merged_dup}")
    print(f"ключей на тему: медиана {sizes[len(sizes) // 2]}, "
          f"максимум {sizes[-1]}, "
          f"в среднем {(kept - n_tail) / len(topics):.1f}")
    print(f"спрос по свободным темам {sum(t['n'] for t in free):,} показов/мес"
          .replace(",", " "))
    print("не взято: " + ", ".join(f"{k} — {v}" for k, v in skipped.items()))
    print()
    for nn in sorted(by_block):
        n_free = sum(1 for t in by_block[nn] if not t["covered"])
        print(f"  {nn} {BLOCKS[nn][0]:24} {len(by_block[nn]):4} тем "
              f"({n_free:3} свободны)")
    print(f"\n→ {OUT}")


if __name__ == "__main__":
    main()
